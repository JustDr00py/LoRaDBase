# Server Configuration
PORT=3001
NODE_ENV=production

# JWT Configuration for UI session tokens (minimum 32 characters)
# Note: This is separate from LoRaDB server API keys, which are managed per-server in the UI
JWT_SECRET=your-32-character-secret-key-here!!!
JWT_EXPIRATION_HOURS=1

# CORS Configuration
CORS_ORIGIN=http://localhost:3000
