# ============================================================================
# LoRaDB UI Configuration
# ============================================================================

# Backend Configuration
BACKEND_PORT=3001

# LoRaDB API URL - Use the public IP/domain of your LoRaDB server
# Examples:
#   - http://192.168.1.100:8080 (local network)
#   - https://loradb.yourdomain.com (with reverse proxy)
#   - http://loradb-server.example.com:8080
LORADB_API_URL=http://YOUR_LORADB_SERVER_IP:8080

# JWT Configuration (must match LoRaDB server)
JWT_SECRET=your-32-character-secret-key-here!!!
JWT_EXPIRATION_HOURS=1

# Frontend Configuration
FRONTEND_PORT=3000

# Frontend API URL - URL where the backend is accessible from your browser
# If accessing locally: http://localhost:3001
# If accessing remotely: http://YOUR_UI_SERVER_IP:3001
VITE_API_URL=http://localhost:3001

# CORS Configuration - Allow frontend origin
# If accessing locally: http://localhost:3000
# If accessing remotely: http://YOUR_UI_SERVER_IP:3000
CORS_ORIGIN=http://localhost:3000
